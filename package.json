{
  "name": "scrape-the-plate",
  "version": "1.0.0",
  "description": "A web app for scraping and collating recipes",
  "scripts": {
    "lint": "yarn lint-django && cd frontend/ && yarn lint && cd ..",
    "format": "yarn format-django && cd frontend/ && yarn format && cd ..",
    "test": "yarn test-django && cd frontend/ && yarn test-nowatch && cd ..",
    "lint-django": "flake8",
    "format-django": ":",
    "test-django": "$VIRTUAL_ENV/bin/python manage.py test",
    "heroku-postbuild": "cd frontend/ && yarn install && yarn run build"
  },
  "pre-commit": [
    "lint",
    "format",
    "test"
  ],
  "cacheDirectories": [
    "node_modules",
    "frontend/node_modules"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/jf248/scrape-the-plate.git"
  },
  "keywords": [
    "recipes"
  ],
  "author": "Joshua Freedman",
  "license": "MIT",
  "homepage": "https://github.com/jf248/scrape-the-plate#readme",
  "devDependencies": {
    "pre-commit": "^1.2.2"
  }
}
